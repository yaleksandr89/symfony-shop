{% apply inky_to_html|inline_css(source('@public' ~ asset('build/email.css'))) %}
    <container>
        <row class="header">
            <columns>
                <a href="{{ url('main_homepage') }}">
                    <center>
                        <img src="{{ email.image('@images/icons/logo_366x79.svg') }}" alt="Symfony shop" class="logo">
                    </center>
                </a>
            </columns>
        </row>

        {% block content %}{% endblock %}

        <spacer size="20"></spacer>

        <row class="footer">
            <columns>
                <p>
                    <center>
                        By Symfony shop (pet projects)
                    </center>
                </p>
            </columns>
        </row>
    </container>
{% endapply %}