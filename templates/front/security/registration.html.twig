{% extends 'front/base.html.twig' %}

{% block title %}{{ 'registration_form.head'|trans }}{% endblock %}

{% block body %}
    <div class="page-content pt-4 pb-4">
        <div class="container">
            <div class="row">
                <div class="col-lg-12">
                    <div class="page-details page-login mt-4">
                        <h1 class="h3 mb-3 text-center font-weight-normal title-gradient-1">
                            {{ 'registration_form.head'|trans }}
                        </h1>
                        {{ form_start(registrationForm) }}

                        <div class="form-group">
                            {{ form_label(registrationForm.email) }}
                            {{ form_widget(registrationForm.email) }}
                            {{ form_errors(registrationForm.email) }}
                        </div>

                        <div class="form-group">
                            {{ form_label(registrationForm.plainPassword) }}
                            {{ form_widget(registrationForm.plainPassword) }}
                            {{ form_errors(registrationForm.plainPassword) }}
                        </div>

                        <div class="form-footer">
                            <div class="row">
                                <div class="col-lg-12">
                                    <button type="submit" class="btn btn btn-success">
                                        {{ 'registration_form.sing_up'|trans }}
                                    </button>
                                </div>
                            </div>
                            <div class="row">
                            <div class="col-lg-12">
                                <div class="custom-control custom-checkbox">
                                    {{ form_widget(registrationForm.agreeTerms) }}
                                        {{ form_label(registrationForm.agreeTerms) }} <a href="#">{{ 'registration_form.privacy policy'|trans }}</a>
                                    {{ form_errors(registrationForm.agreeTerms) }}
                                </div>
                            </div>
                            </div>
                        </div>

                        {{ form_end(registrationForm) }}

                        {% include 'front/_embed/_menu/_login_via_social_network.html.twig' %}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
