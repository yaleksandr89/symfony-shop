{% extends '@Admin/layout/layout_main.html.twig' %}

{% block title_detailed %}{{ 'dashboard.page_title'|trans({}, 'admin') }}{% endblock %}

{% block body %}
    <div class="container-fluid">

        <div class="d-sm-flex align-items-center justify-content-between mb-4">
            <h1 class="h3 mb-0 text-gray-800">{{ 'dashboard.heading'|trans({}, 'admin') }}</h1>
        </div>

        <div class="row"></div>

    </div>
{% endblock %}
